pipeline {
    agent {
        label 'cpvm'
    }
    stages {
        agent {
            label 'cpvm'
        }
        stage('Build') { 
            steps {
                sh 'npm install' 
            }
        }
    }
}